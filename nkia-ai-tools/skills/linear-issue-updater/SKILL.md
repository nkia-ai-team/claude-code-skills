---
name: linear-issue-updater
description: Update Linear issue descriptions by adding, modifying, or removing DoD (Definition of Done) and AC (Acceptance Criteria) items. Use this skill when work scope changes during development, new requirements emerge, or existing criteria need modification.
---

# Linear Issue Updater

## Overview

진행 중인 Linear 이슈의 작업 내용과 DoD/AC를 업데이트하는 스킬입니다. 작업 범위 변경, 추가 요구사항, 기존 항목 수정 등을 지원합니다.

**주요 기능:**
1. DoD 항목 추가/수정/삭제
2. AC 항목 추가/수정/삭제
3. 작업 배경/설명 업데이트
4. 변경 이력 코멘트 자동 생성

## Usage

### Interactive Mode (기본)

```
/linear-issue-updater <issue-id>
```

**Examples:**
```
/linear-issue-updater NKIAAI-137
/linear-issue-updater https://linear.app/nkia-ai/issue/NKIAAI-137
```

### Quick Add Mode

```
/linear-issue-updater <issue-id> --add-dod "새로운 DoD 항목"
/linear-issue-updater <issue-id> --add-ac "새로운 AC 항목"
```

### Auto Mode (자연어 기반)

```
/linear-issue-updater <issue-id> --auto
```

또는 자연어 설명과 함께:
```
/linear-issue-updater NKIAAI-137 --auto "GitLab MR에서 대용량 파일을 읽지 못하는 문제가 있어서 별도로 전체 파일을 조회하는 로직이 필요해"
```

---

## Auto Mode Workflow

**자연어로 추가 작업을 설명하면 자동으로 DoD/AC 업데이트를 추천합니다.**

### Auto Step 1: Fetch Current Issue

`mcp__linear__get_issue`를 사용하여 현재 이슈 정보와 기존 DoD/AC를 가져옵니다.

```
=== 현재 이슈 정보 ===

제목: code-review 및 commit 스킬 Linear 이슈번호 형식 적용
상태: In Progress

현재 DoD:
1. [ ] code-review 스킬 브랜치명 검증 패턴 수정 → 결과물: PR 링크
2. [ ] commit 스킬 커밋 메시지 형식 수정 → 결과물: PR 링크

현재 AC:
1. [ ] feature/nkiaai-XXX 브랜치명 인식 → 결과물: 테스트 결과
2. [ ] nkiaai-XXX Feat : 형식 커밋 메시지 생성 → 결과물: 테스트 결과

=======================
```

### Auto Step 2: Collect Natural Language Input

자연어로 추가 작업 내용을 입력받습니다.

```
추가 작업 내용을 자연어로 설명해주세요:

예시:
"GitLab MR에서 대용량 파일 diff가 잘려서 나오는 문제가 있어.
파일 크기가 크면 별도 API로 전체 내용을 조회하는 로직을 추가해야 해.
그리고 리뷰 결과에 대용량 파일임을 표시해줘야 함."

입력:
```

### Auto Step 3: Analyze and Generate Suggestions

LLM이 자연어 입력과 현재 이슈 맥락을 분석하여 DoD/AC 추가/수정 사항을 추천합니다.

**분석 기준:**
1. **새로운 작업 식별**: 자연어에서 새로 해야 할 작업 추출
2. **기존 항목 연관성**: 현재 DoD/AC와 중복되거나 확장되는 내용 확인
3. **적절한 분류**: DoD(프로세스 완료)와 AC(품질 기준) 구분
4. **결과물 정의**: 각 항목의 검증 가능한 결과물 제안

**추천 항목 형식:**
```json
{
  "analysis": {
    "summary": "GitLab 대용량 파일 처리 기능 추가 필요",
    "related_existing": ["DoD #1 - code-review 스킬 수정에 포함 가능"]
  },
  "suggestions": {
    "add_dod": [
      {
        "content": "GitLab 대용량 파일 전체 조회 로직 구현",
        "evidence": "PR 링크 {{pr_link}}",
        "priority": null,
        "reason": "diff가 잘릴 때 별도 API로 파일 내용 조회 필요"
      }
    ],
    "add_ac": [
      {
        "content": "500줄 이상 파일 전체 내용 조회 가능",
        "evidence": "MR 리뷰 결과 {{review_link}}",
        "priority": "[필수]",
        "reason": "대용량 파일도 리뷰 가능해야 함"
      },
      {
        "content": "리뷰 결과에 '📦 대용량 파일' 표시",
        "evidence": "리뷰 코멘트 스크린샷 {{screenshot}}",
        "priority": null,
        "reason": "사용자에게 대용량 파일임을 명시"
      }
    ],
    "modify_dod": [],
    "modify_ac": []
  }
}
```

### Auto Step 4: Present Suggestions

추천된 변경사항을 사용자 친화적으로 표시합니다.

```
=== 추천 변경사항 ===

📊 분석 결과:
- 요약: GitLab 대용량 파일 처리 기능 추가 필요
- 관련 기존 항목: DoD #1 (code-review 스킬 수정)과 연관됨

📝 DoD 추가 추천 (1개):
┌─────────────────────────────────────────────────────────────┐
│ 1. GitLab 대용량 파일 전체 조회 로직 구현                        │
│    → 결과물: PR 링크 {{pr_link}}                               │
│    💡 사유: diff가 잘릴 때 별도 API로 파일 내용 조회 필요          │
└─────────────────────────────────────────────────────────────┘

✅ AC 추가 추천 (2개):
┌─────────────────────────────────────────────────────────────┐
│ 1. **[필수]** 500줄 이상 파일 전체 내용 조회 가능               │
│    → 결과물: MR 리뷰 결과 {{review_link}}                      │
│    💡 사유: 대용량 파일도 리뷰 가능해야 함                        │
├─────────────────────────────────────────────────────────────┤
│ 2. 리뷰 결과에 '📦 대용량 파일' 표시                            │
│    → 결과물: 리뷰 코멘트 스크린샷 {{screenshot}}                 │
│    💡 사유: 사용자에게 대용량 파일임을 명시                       │
└─────────────────────────────────────────────────────────────┘

======================

어떻게 하시겠습니까?
1. 전체 적용
2. 선택적 적용 (항목별 선택)
3. 수정 후 적용
4. 다시 분석
5. 취소

선택:
```

### Auto Step 5: Selective Application (옵션 2 선택 시)

```
적용할 항목을 선택하세요 (쉼표로 구분):

DoD 추가:
  [1] GitLab 대용량 파일 전체 조회 로직 구현

AC 추가:
  [2] **[필수]** 500줄 이상 파일 전체 내용 조회 가능
  [3] 리뷰 결과에 '📦 대용량 파일' 표시

선택 (예: 1,2 또는 all):
```

### Auto Step 6: Edit Before Apply (옵션 3 선택 시)

```
수정할 항목 번호를 선택하세요:

[1] DoD: GitLab 대용량 파일 전체 조회 로직 구현
[2] AC: 500줄 이상 파일 전체 내용 조회 가능
[3] AC: 리뷰 결과에 '📦 대용량 파일' 표시

선택: 2

현재 내용:
**[필수]** 500줄 이상 파일 전체 내용 조회 가능 → 결과물: MR 리뷰 결과 {{review_link}}

수정된 내용을 입력하세요:
> **[필수]** 1000줄 이상 파일 전체 내용 조회 가능 → 결과물: MR 리뷰 결과 {{review_link}}

수정 완료. 계속 수정하시겠습니까? (y/n)
```

### Auto Step 7: Preview and Confirm

```
=== 적용될 변경사항 미리보기 ===

📝 DoD 변경:
  ➕ 추가: GitLab 대용량 파일 전체 조회 로직 구현 → 결과물: PR 링크 {{pr_link}}

✅ AC 변경:
  ➕ 추가: **[필수]** 1000줄 이상 파일 전체 내용 조회 가능 → 결과물: MR 리뷰 결과 {{review_link}}
  ➕ 추가: 리뷰 결과에 '📦 대용량 파일' 표시 → 결과물: 리뷰 코멘트 스크린샷 {{screenshot}}

--- 변경 후 전체 DoD ---
- [ ] code-review 스킬 브랜치명 검증 패턴 수정 → 결과물: PR 링크
- [ ] commit 스킬 커밋 메시지 형식 수정 → 결과물: PR 링크
- [ ] GitLab 대용량 파일 전체 조회 로직 구현 → 결과물: PR 링크 {{pr_link}}  ← NEW

--- 변경 후 전체 AC ---
- [ ] feature/nkiaai-XXX 브랜치명 인식 → 결과물: 테스트 결과
- [ ] nkiaai-XXX Feat : 형식 커밋 메시지 생성 → 결과물: 테스트 결과
- [ ] **[필수]** 1000줄 이상 파일 전체 내용 조회 가능 → 결과물: MR 리뷰 결과 {{review_link}}  ← NEW
- [ ] 리뷰 결과에 '📦 대용량 파일' 표시 → 결과물: 리뷰 코멘트 스크린샷 {{screenshot}}  ← NEW

========================

적용하시겠습니까?
1. 예, 적용하고 변경 코멘트 추가
2. 예, 적용만 (코멘트 없음)
3. 아니오, 다시 편집

선택:
```

### Auto Step 8: Apply and Comment

선택에 따라 Description을 업데이트하고 변경 코멘트를 생성합니다.

```
✅ 이슈가 업데이트되었습니다!

이슈: NKIAAI-137
제목: code-review 및 commit 스킬 Linear 이슈번호 형식 적용

적용된 변경:
- DoD: 1개 추가
- AC: 2개 추가
- 변경 코멘트 작성됨

Linear URL: https://linear.app/nkia-ai/issue/NKIAAI-137
```

---

## Auto Mode Analysis Guidelines

### 작업 유형별 추천 패턴

**코드 변경 작업:**
- DoD: 코드 구현, PR 생성, 코드 리뷰
- AC: 기능 동작 확인, 테스트 통과, 성능 기준

**버그 수정 작업:**
- DoD: 원인 분석, 수정 코드, 재발 방지 테스트
- AC: 재현 불가 확인, 관련 에러 0건

**문서 작업:**
- DoD: 문서 작성, 리뷰 완료
- AC: 필수 섹션 포함, 예시 코드 동작 확인

**설정/배포 작업:**
- DoD: 설정 파일 수정, 배포 완료
- AC: 환경 정상 동작, Healthcheck 통과

### 결과물 추천 규칙

| 작업 유형 | 추천 결과물 |
|----------|-----------|
| 코드 작업 | PR 링크, 테스트 결과 |
| 문서 작업 | 문서 링크, 리뷰 기록 |
| 배포 작업 | 배포 로그, Healthcheck 결과 |
| 데이터 작업 | 데이터 경로, 품질 리포트 |
| 리서치 작업 | 조사 문서, 결론 요약 |

### 기존 항목 연관성 분석

자연어 입력을 분석할 때 기존 DoD/AC와의 관계를 파악합니다:

1. **확장 관계**: 기존 항목을 더 구체화하거나 세분화
   - "대용량 파일 처리" → 기존 "code-review 스킬 수정"의 세부 작업

2. **의존 관계**: 기존 항목이 선행되어야 하는 경우
   - "테스트 작성" → 기존 "기능 구현" 완료 후 진행

3. **독립 관계**: 기존 항목과 별개의 새로운 작업
   - 새로운 요구사항 추가

---

## Interactive Mode Workflow

**수동으로 DoD/AC 항목을 관리하는 기본 모드입니다.**

### Step 1: Fetch Issue Details

`mcp__linear__get_issue`를 사용하여 현재 이슈 정보를 가져옵니다.

```
=== 이슈 정보 ===

제목: code-review 및 commit 스킬 Linear 이슈번호 형식 적용
상태: In Progress
담당자: 홍길동

--- 현재 Description ---

## 배경
/code-review와 /commit 스킬이 PIMS 번호 형식을 사용하고 있어 Linear 이슈 번호 형식으로 변경 필요

## Definition of Done (DoD)
- [ ] code-review 스킬 브랜치명 검증 패턴 수정 → 결과물: PR 링크
- [ ] commit 스킬 커밋 메시지 형식 수정 → 결과물: PR 링크

## Acceptance Criteria (AC)
- [ ] feature/nkiaai-XXX 브랜치명 인식 → 결과물: 테스트 결과
- [ ] nkiaai-XXX Feat : 형식 커밋 메시지 생성 → 결과물: 테스트 결과

==================
```

### Step 2: Show Update Options

```
어떤 작업을 하시겠습니까?

1. DoD 항목 추가
2. DoD 항목 수정
3. DoD 항목 삭제
4. AC 항목 추가
5. AC 항목 수정
6. AC 항목 삭제
7. 배경/설명 수정
8. 전체 Description 직접 편집
9. 변경사항 확인 및 저장
0. 취소

선택:
```

---

## DoD/AC 항목 형식

### 기본 형식

```markdown
- [ ] [작업 내용] → 결과물: [증빙 자료]
```

### 우선순위 태그 (선택)

```markdown
- [ ] **[필수]** [작업 내용] → 결과물: [증빙]
- [ ] **[공통]** [작업 내용] → 결과물: [증빙]
- [ ] **[옵셔널]** [작업 내용] → 결과물: [증빙]
```

### 템플릿 변수 사용

```markdown
- [ ] 목표 데이터 {{record_count}}건 수집 → 결과물: {{data_path}}
- [ ] API 응답 시간 {{max_response_time}}ms 이하 → 결과물: {{perf_result}}
```

---

## Update Operations

### Operation 1: DoD 항목 추가

```
=== DoD 항목 추가 ===

현재 DoD 항목:
1. [ ] code-review 스킬 브랜치명 검증 패턴 수정
2. [ ] commit 스킬 커밋 메시지 형식 수정

새로운 DoD 항목을 입력하세요:
(형식: [작업 내용] → 결과물: [증빙])

입력: 대용량 파일 처리 로직 추가 → 결과물: PR 링크 {{pr_link}}

우선순위 태그를 선택하세요:
1. 없음 (기본)
2. **[필수]**
3. **[공통]**
4. **[옵셔널]**

선택: 1

추가할 DoD 항목:
- [ ] 대용량 파일 처리 로직 추가 → 결과물: PR 링크 {{pr_link}}

추가하시겠습니까? (y/n)
```

**API 호출:**
```javascript
// 1. 현재 description 가져오기
const issue = await mcp__linear__get_issue({ id: "issue-id" });
let description = issue.description;

// 2. DoD 섹션 찾아서 항목 추가
const dodSection = "## Definition of Done (DoD)";
const newItem = "- [ ] 대용량 파일 처리 로직 추가 → 결과물: PR 링크 {{pr_link}}";

// DoD 섹션 끝에 항목 추가
description = insertAfterSection(description, dodSection, newItem);

// 3. 업데이트
await mcp__linear__update_issue({
  id: "issue-id",
  description: description
});
```

### Operation 2: DoD 항목 수정

```
=== DoD 항목 수정 ===

수정할 항목을 선택하세요:
1. [ ] code-review 스킬 브랜치명 검증 패턴 수정 → 결과물: PR 링크
2. [ ] commit 스킬 커밋 메시지 형식 수정 → 결과물: PR 링크
3. [ ] 대용량 파일 처리 로직 추가 → 결과물: PR 링크 {{pr_link}}

선택: 1

현재 내용:
code-review 스킬 브랜치명 검증 패턴 수정 → 결과물: PR 링크

수정할 부분을 선택하세요:
1. 작업 내용 수정
2. 결과물 수정
3. 전체 수정
4. 체크 상태 변경 ([ ] ↔ [x])

선택: 1

새로운 작업 내용:
code-review 스킬 브랜치명/커밋메시지 검증 패턴을 Linear 형식으로 수정

수정된 DoD 항목:
- [ ] code-review 스킬 브랜치명/커밋메시지 검증 패턴을 Linear 형식으로 수정 → 결과물: PR 링크

적용하시겠습니까? (y/n)
```

### Operation 3: DoD 항목 삭제

```
=== DoD 항목 삭제 ===

삭제할 항목을 선택하세요:
1. [ ] code-review 스킬 브랜치명 검증 패턴 수정
2. [ ] commit 스킬 커밋 메시지 형식 수정
3. [ ] 대용량 파일 처리 로직 추가

선택: 3

⚠️ 다음 항목을 삭제합니다:
- [ ] 대용량 파일 처리 로직 추가 → 결과물: PR 링크 {{pr_link}}

삭제 사유를 입력하세요 (선택사항):
> 요구사항 변경으로 제외됨

삭제하시겠습니까? (y/n)
```

### Operation 4-6: AC 항목 추가/수정/삭제

DoD와 동일한 방식으로 AC 섹션에 대해 수행합니다.

```
=== AC 항목 추가 ===

현재 AC 항목:
1. [ ] feature/nkiaai-XXX 브랜치명 인식
2. [ ] nkiaai-XXX Feat : 형식 커밋 메시지 생성

새로운 AC 항목을 입력하세요:
입력: GitLab MR에서 대용량 파일(+500 lines) 내용 조회 가능 → 결과물: MR 리뷰 결과 {{review_link}}
```

### Operation 7: 배경/설명 수정

```
=== 배경/설명 수정 ===

현재 배경:
/code-review와 /commit 스킬이 PIMS 번호 형식을 사용하고 있어 Linear 이슈 번호 형식으로 변경 필요

수정할 내용을 입력하세요 (Markdown 지원):
```

### Operation 8: 전체 Description 직접 편집

```
=== 전체 Description 편집 ===

현재 Description이 클립보드에 복사되었습니다.
편집 후 붙여넣기 하세요:

--- 현재 내용 ---
## 배경
[현재 내용]

## Definition of Done (DoD)
[현재 항목들]

## Acceptance Criteria (AC)
[현재 항목들]
---

수정된 Description을 입력하세요 (완료 시 빈 줄 2번 입력):
```

### Operation 9: 변경사항 확인 및 저장

```
=== 변경사항 미리보기 ===

📝 변경 내용:

DoD 변경:
  ➕ 추가: 대용량 파일 처리 로직 추가
  ✏️ 수정: code-review 스킬 브랜치명 검증 패턴 수정 → code-review 스킬 브랜치명/커밋메시지 검증 패턴을 Linear 형식으로 수정

AC 변경:
  ➕ 추가: GitLab MR에서 대용량 파일 내용 조회 가능

--- 변경 후 Description ---
[새로운 Description 전체 표시]
---

저장하시겠습니까?
1. 예, 저장하고 변경 코멘트 추가
2. 예, 저장만 (코멘트 없음)
3. 아니오, 계속 편집

선택:
```

---

## Auto-Generated Change Comment

변경사항 저장 시 자동으로 코멘트를 생성합니다.

```markdown
## 📝 이슈 내용 업데이트

**변경일시:** 2025-01-29 15:30

### DoD 변경
| 유형 | 항목 |
|-----|------|
| ➕ 추가 | 대용량 파일 처리 로직 추가 |
| ✏️ 수정 | code-review 스킬 브랜치명 검증 패턴 수정 → code-review 스킬 브랜치명/커밋메시지 검증 패턴을 Linear 형식으로 수정 |

### AC 변경
| 유형 | 항목 |
|-----|------|
| ➕ 추가 | GitLab MR에서 대용량 파일 내용 조회 가능 |

### 변경 사유
작업 진행 중 GitLab 대용량 파일 diff 처리 이슈 발견으로 추가 작업 필요

---
*🤖 Generated by Linear Issue Updater*
```

**API 호출:**
```javascript
await mcp__linear__create_comment({
  issueId: "issue-uuid",
  body: changeCommentMarkdown
});
```

---

## Command Options

### Quick Add

```bash
# DoD 항목 빠른 추가
/linear-issue-updater NKIAAI-137 --add-dod "새로운 작업 → 결과물: {{link}}"

# AC 항목 빠른 추가
/linear-issue-updater NKIAAI-137 --add-ac "새로운 기준 → 결과물: {{evidence}}"

# 우선순위 태그와 함께 추가
/linear-issue-updater NKIAAI-137 --add-dod "[필수] 중요 작업 → 결과물: PR"
```

### Auto Mode

```bash
# Auto Mode 진입 (자연어 입력 요청)
/linear-issue-updater NKIAAI-137 --auto

# 자연어 설명과 함께 Auto Mode
/linear-issue-updater NKIAAI-137 --auto "대용량 파일 처리 로직을 추가해야 해"

# Auto Mode + 자동 적용 (확인 없이)
/linear-issue-updater NKIAAI-137 --auto "에러 핸들링 추가" --apply
```

### Options

| 옵션 | 설명 | 예시 |
|-----|------|------|
| `--add-dod` | DoD 항목 추가 | `--add-dod "작업 내용"` |
| `--add-ac` | AC 항목 추가 | `--add-ac "품질 기준"` |
| `--auto` | 자연어 기반 Auto Mode | `--auto "추가 작업 설명"` |
| `--apply` | Auto Mode에서 확인 없이 적용 | `--auto "..." --apply` |
| `--no-comment` | 변경 코멘트 생략 | `--no-comment` |
| `--reason` | 변경 사유 지정 | `--reason "요구사항 변경"` |

---

## Description Structure

### Expected Format

스킬은 다음 구조의 Description을 파싱하고 업데이트합니다:

```markdown
## 배경
[작업 배경 설명]

## 작업 설명
[상세 작업 내용]

## Definition of Done (DoD)
- [ ] **[필수]** [작업 내용 1] → 결과물: [증빙]
- [ ] **[공통]** [작업 내용 2] → 결과물: [증빙]
- [ ] [작업 내용 3] → 결과물: [증빙]

## Acceptance Criteria (AC)
- [ ] **[필수]** [품질 기준 1] → 결과물: [증빙]
- [ ] [품질 기준 2] → 결과물: [증빙]

## 참고사항
[추가 정보]
```

### Section Detection

다음 패턴으로 섹션을 인식합니다:

| 섹션 | 인식 패턴 |
|-----|----------|
| 배경 | `## 배경`, `## Background` |
| 작업 설명 | `## 작업 설명`, `## Description`, `## 설명` |
| DoD | `## Definition of Done`, `## DoD`, `## 완료 정의` |
| AC | `## Acceptance Criteria`, `## AC`, `## 인수 조건` |
| 참고 | `## 참고사항`, `## Notes`, `## 참고` |

---

## Parsing Logic

### DoD/AC 항목 파싱

```javascript
function parseChecklistItems(description, sectionHeader) {
  // 섹션 찾기
  const sectionRegex = new RegExp(`${sectionHeader}[\\s\\S]*?(?=##|$)`, 'i');
  const sectionMatch = description.match(sectionRegex);

  if (!sectionMatch) return [];

  // 체크박스 항목 파싱
  const itemRegex = /- \[([ x])\] (\*\*\[.+?\]\*\* )?(.+?)(?= → 결과물:| → |$)/g;
  const items = [];

  let match;
  while ((match = itemRegex.exec(sectionMatch[0])) !== null) {
    items.push({
      checked: match[1] === 'x',
      priority: match[2]?.replace(/\*\*/g, '').trim() || null,
      content: match[3].trim(),
      evidence: extractEvidence(match.input, match.index)
    });
  }

  return items;
}
```

### 항목 업데이트

```javascript
function updateItem(description, sectionHeader, itemIndex, newContent) {
  const items = parseChecklistItems(description, sectionHeader);

  if (itemIndex >= items.length) {
    throw new Error('Invalid item index');
  }

  const oldItem = items[itemIndex];
  const newItemText = formatChecklistItem(newContent);

  // 기존 항목을 새 항목으로 교체
  return description.replace(
    formatChecklistItem(oldItem),
    newItemText
  );
}
```

---

## Error Handling

### Parsing Errors

```
⚠️ Description 형식을 파싱할 수 없습니다.

DoD/AC 섹션이 없거나 형식이 다릅니다.
전체 편집 모드로 전환하시겠습니까? (y/n)
```

### Invalid Item Format

```
⚠️ 항목 형식이 올바르지 않습니다.

입력: 그냥 텍스트
권장 형식: [작업 내용] → 결과물: [증빙]

형식에 맞게 다시 입력해주세요:
```

### Conflict Detection

```
⚠️ 이슈가 다른 곳에서 수정되었습니다.

마지막 조회: 2025-01-29 15:00
현재 수정 시간: 2025-01-29 15:10

어떻게 하시겠습니까?
1. 최신 내용 다시 불러오기 (내 변경사항 유지)
2. 최신 내용으로 새로 시작
3. 강제 저장 (주의: 다른 변경 덮어씀)

선택:
```

---

## Integration with Other Skills

### With `/linear-issue-creator`

이슈 생성 후 작업 중 변경사항 반영:
```
/linear-issue-creator  → 초기 DoD/AC 생성
[작업 진행 중 추가 요구사항 발견]
/linear-issue-updater NKIAAI-138 --add-dod "추가 작업"
```

### With `/linear-issue-validator`

검증 실패 항목 수정:
```
/linear-issue-validator NKIAAI-137  → AC #2 실패
/linear-issue-updater NKIAAI-137  → AC #2 수정
/linear-issue-validator NKIAAI-137  → 재검증
```

---

## Best Practices

### 1. 변경 사유 기록

항목 추가/수정/삭제 시 변경 사유를 기록하면 나중에 추적이 용이합니다.

```
/linear-issue-updater NKIAAI-137 --add-dod "새 작업" --reason "스프린트 중 요구사항 추가"
```

### 2. 우선순위 태그 활용

- **[필수]**: 반드시 완료해야 하는 핵심 항목
- **[공통]**: 모든 작업에 적용되는 표준 항목 (코드 리뷰 등)
- **[옵셔널]**: 시간이 허락하면 하는 선택 항목

### 3. 템플릿 변수 사용

구체적인 값은 검증 시점에 채우도록 템플릿 변수 활용:

```markdown
- [ ] 목표 데이터 {{record_count}}건 수집 → 결과물: {{data_path}}
```

### 4. 작은 단위로 업데이트

대규모 변경보다 작은 단위로 자주 업데이트하면 변경 이력 추적이 쉽습니다.

---

## Output Summary

### 성공

```
✅ 이슈가 업데이트되었습니다!

이슈: NKIAAI-137
제목: code-review 및 commit 스킬 Linear 이슈번호 형식 적용

변경 내용:
- DoD: 1개 추가, 1개 수정
- AC: 1개 추가
- 변경 코멘트 작성됨

Linear URL: https://linear.app/nkia-ai/issue/NKIAAI-137
```

### 실패

```
❌ 이슈 업데이트에 실패했습니다.

오류: {error_message}
```
