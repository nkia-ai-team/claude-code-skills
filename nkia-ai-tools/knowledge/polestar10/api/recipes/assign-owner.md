# Recipe: 담당자 권한 부여 (TBD)

관리대상 또는 조직 단위에 담당자 권한을 부여.

- **엔드포인트**: **TBD** (추정 prefix: `/api/account/*`)
- **인증 필요**: `recipes/login.md` 먼저 실행
- **부작용**: 권한 변경 (감사 대상)

## ⚠️ 현재 상태: 미확정

## 확정 절차

DevTools 로 녹화:

1. 정상 로그인 후 **시스템 관리 > 사용자/권한** (또는 조직도 관리 메뉴) 진입
2. 사용자 선택 → **권한 부여** 또는 **역할 변경** 액션 수행
3. DevTools Network 에서 발생한 `POST` 요청 식별
4. URL + payload 확인 (대상 관리대상 ID · 사용자 ID · 역할 코드)

## 레시피 (확정 후 작성)

```bash
# TODO: DevTools 캡처 후 아래 블록 작성
```

## UI Fallback

> **시스템 관리 > 사용자/권한 관리** (또는 조직 > 역할) 에서 수동 권한 부여.
